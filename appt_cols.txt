id (int(11))
patient_id (int(11))
doctor_id (int(11))
appointment_date (datetime)
status (enum('booked','pending','confirmed','cancelled','completed'))
symptom_description (text)
meeting_link (varchar(255))
created_at (timestamp)
scheduled_date (date)
scheduled_time (time)
payment_status (enum('unpaid','paid','refunded','pending'))
consultation_fee (decimal(10,2))
notes (text)
cancellation_reason (text)
payment_transaction_id (int(11))
